[project]
name = "voltron"
version = "0.1.0"
description = "Parallel Claude Code agent dispatcher — CLI + worker daemon"
requires-python = ">=3.11"
dependencies = [
    "aiosqlite>=0.20.0",
]

[project.scripts]
voltron = "src.cli:main"
